import 'package:flutter/material.dart';

class PPTsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('PPTs')),
      body: Center(
        child: Text('PPTs Screen'),
      ),
    );
  }
}